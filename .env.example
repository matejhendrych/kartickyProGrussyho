# Application Security
# Generate a secure key: python3 -c 'import os; import base64; print(base64.b64encode(os.urandom(32)).decode())'
APP_KEY=CHANGE-THIS-TO-A-RANDOM-SECRET-KEY
APP_DEBUG=False

# Database Configuration
# PostgreSQL (recommended)
DATABASE_URL=postgresql://karty:karty@localhost/karty

# OR MySQL (supported)
# DATABASE_URL=mysql+pymysql://karty:karty@localhost/karty?charset=utf8mb4

# OR SQLite (development only)
# DATABASE_URL=sqlite:///dev.db

# MQTT Broker Configuration
MQTT_BROKER=192.168.1.110
MQTT_PORT=1883

# Email Configuration (optional)
APP_MAIL_USERNAME=your-email@gmail.com
APP_MAIL_PASSWORD=your-app-specific-password
APP_MAIL_INFO_ACCOUNT=your-email@gmail.com
APP_TEST_RECIPIENT=test@example.com

# Upload Configuration
UPLOAD_FOLDER=uploads/
